# FilmWishlist — Telegram Mini App

Личный вишлист фильмов/сериалов с рекомендациями и социалкой (профиль, друзья, реки от друзей). Добавление фильмов через чат с ботом с подтверждением.

## Решения (зафиксировано)

| Вопрос | Решение |
|--------|---------|
| Стек | Next.js (App Router, TS) + React, бот на grammY (Node/TS) |
| Данные о фильмах | TMDB API (`language=ru-RU`) |
| Рекомендации | TMDB `recommendations`/`similar` по вишлисту + «новинки/популярное» |
| Scope | Социалка сразу (профиль, друзья, реки) |
| Оценки | ⭐ 5 звёзд |
| Любимые фильмы | Отдельный список (витрина в профиле) |
| Бот-добавление | Топ-3 карточки с постером → кнопка «Этот» |
| Друзья | Инвайт-ссылка + поиск по username |
| Реки | Личный инбокс + лента активности |
| «Где посмотреть» (стриминги) | ❌ не делаем |
| Приватность | Друзья видят всё |
| Уведомления | Река от друга · новинки по вкусу · новый друг · напоминание посмотреть |
| Язык интерфейса | Русский |
| Кадэнс уведомлений | Новинки — раз в неделю (пн утром); напоминание — после 7 дней тишины; всё отключаемо |
| Масштаб | Я + пара друзей, с заделом на рост |

## Архитектура

| Слой | Технология |
|------|-----------|
| Mini App (фронт) | Next.js + React, `@telegram-apps/sdk-react` |
| Бот | Node + grammY |
| API | Next.js API routes (одна кодовая база) |
| БД | Postgres (Neon/Supabase) через Prisma |
| Авторизация | Telegram `initData` (проверка HMAC-подписи по bot token) |
| Хостинг | Vercel (фронт+API) + Neon (БД); бот — отдельный процесс (Railway/VPS) |

## Модель данных

```
users        (id, tg_id, username, name, avatar_url, notif_prefs jsonb, created_at)
titles       (id, tmdb_id, type[movie|tv], title, year, poster, genres, overview)  — кэш TMDB
wishlist     (user_id, title_id, status[want|watching|watched], stars 1-5, added_at)
favorites    (user_id, title_id, position)
friendships  (user_a, user_b, status[pending|accepted], created_at)
recs         (from_user, to_user, title_id, comment, status[new|added|dismissed])
activity     (user_id, type, title_id, created_at)
```

## Экраны Mini App

1. **Вишлист** — фильтры хочу/смотрю/посмотрел, ⭐-оценка, поиск.
2. **Рекомендации** — «по вишлисту» + «новинки/популярное».
3. **Инбокс рек** — что друзья прислали лично тебе → в 1 тап в вишлист.
4. **Лента друзей** — активность друзей, «взять себе».
5. **Профиль** — любимые (витрина), статистика, поделиться.
6. **Друзья** — инвайт-ссылка + поиск, запросы.

## Бот: добавление по названию (топ-3 с подтверждением)

```
Ты:  Дюна
Бот: Нашёл несколько — какой добавить в вишлист?
     [🎬 Дюна (2021)]     ← постер + год, кнопка «Этот»
     [🎬 Дюна (1984)]
     [🎬 Дюна: Часть вторая (2024)]
     [❌ Ничего из этого]
Ты:  *жмёшь «Дюна (2021)»*
Бот: ✅ Добавил «Дюна (2021)» в твой вишлист.
```

## Порядок сборки

- **Фаза 1 (ядро):** Telegram-авторизация, TMDB-поиск, вишлист CRUD + статусы/⭐, бот-добавление топ-3.
- **Фаза 2:** рекомендации (2 вкладки) + единый Apple-стиль эмодзи на всех устройствах (см. [BACKLOG.md](./BACKLOG.md)).
- **Фаза 3:** профиль, любимые, друзья (инвайт + поиск), реки-инбокс, лента активности, уведомления.

## Переменные окружения (`.env`)

```
DATABASE_URL=            # Postgres (Neon/Supabase)
TELEGRAM_BOT_TOKEN=      # @BotFather
TMDB_API_KEY=            # themoviedb.org → Settings → API (v4 read token)
NEXT_PUBLIC_APP_URL=     # публичный URL мини-аппа (для инвайт-ссылок)
```
